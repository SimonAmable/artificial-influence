Always make copy on the site user facings, focus on user benifit and emotional/personal value instead of including technical detail.

Never build or run dev server to verify new features. only run tsx and lint to verify new features. small few line changes dont need verification.

Keep UI/UX minimalistic, intuative, and following my brand/design system always using variables instead of hard coded colors.